const Project = require('./projects-model')

class Validade {
  projectId = async (req, res, next) => {
    const {id} = req.params
    try {
    } catch (error) {}
  }
}

module.exports = new Validade()
